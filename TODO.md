# TODO

- [x] Update `requirements.txt` to allow Python 3.12-compatible versions.
- [x] Ensure `runtime.txt` specifies `python-3.12`.
- [ ] Commit changes (`requirements.txt`, `runtime.txt`) and push to GitHub.
- [ ] Reboot app / redeploy on Streamlit Cloud.

